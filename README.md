# 轻量工具箱 v5

LiteLoaderQQNT / QwQNT 插件 - 轻量工具箱

轻量 · 优雅 · 高效

> [!CAUTION]
> 不要在国内平台宣传该插件。
> 不要在 QQ 官方群聊发送任何能明显看出你使用了第三方插件的截图。

> [!NOTE]
> `v5` 是一次删减后的重构版本，和 `v4` 相比移除了不少历史功能。

> [!TIP]
> 当前分支仍在持续开发中，功能和兼容性请以实际 Release、设置页和提交记录为准。


### 依赖

根据你使用的加载器，需要先安装对应的前置依赖插件

#### qwqnt

- [qwqnt-hako](https://github.com/qwqnt-community/qwqnt-hako)
- [ipc-interceptor](https://github.com/qwqnt-community/qwqnt-ipc-interceptor)

#### LiteLoaderQQNT

- [ipc-interceptor](https://github.com/qwqnt-community/qwqnt-ipc-interceptor)

## 安装方法

### 从 Releases 安装

- 下载 [Releases](https://github.com/xiyuesaves/LiteLoaderQQNT-lite_tools/releases) 中对应版本的压缩包
- 将压缩包内容解压到加载器数据目录下的 `plugins/lite-tools` 文件夹
- 重启 QQ 后，在插件设置中启用并配置

当前构建产物文件名格式为 `lite-tools v<version>.zip`。

### 手动构建

```bash
git clone git@github.com:xiyuesaves/lite_tools.git
cd lite_tools
pnpm install
pnpm run build
```

如果是 alpha 版本构建，可使用：

```bash
pnpm run build:alpha
```

## 使用说明

- 打开设置，进入 `轻量工具箱`
- 大部分功能在设置页内直接开关或填写参数即可
- 标注了“重载消息生效”或“重启生效”的选项，需要额外刷新对应界面
- `顶部功能栏` 和 `聊天功能栏` 需要先进入一次对应界面，设置页才会自动读取并显示可精简的项目

## 功能列表

### 界面调整

- 图片查看器优化
- 侧键返回主列表
- 阻止消息窗口拖拽操作
- 删除消息气泡装扮
- 隐藏天气按钮
- 隐藏经典模式切换按钮
- 隐藏锁定按钮
- 隐藏退出账号按钮
- 隐藏检查更新按钮和更新通知
- 隐藏 VIP 彩色昵称

### 消息相关

- 移除回复 `@`
- 替换小程序卡片为网页卡片
- 解除商城表情限制
- 消息 `+1`
- `+1` 双击执行
- 显示每条消息发送时间
- 显示时区
- 自定义时间格式
- 自定义日期格式

### 阻止撤回

- 拦截撤回消息
- 可选拦截自己的撤回操作
- 持久化保存撤回记录
- 重定向撤回图片保存路径
- 查看重定向图片目录
- 自定义撤回提示颜色
- 清理本地撤回缓存
- 查看撤回消息列表

### 本地贴纸

- 启用本地贴纸面板
- 选择贴纸目录
- 将入口移动到左侧
- 拖拽排序贴纸集
- 以图片形式发送贴纸
- 调整每行贴纸数量
- 调整面板宽度和高度
- 下载 Telegram 贴纸
- 配置 Telegram Bot Token
- 配置 `ffmpeg` 路径
- 配置 `tgsToGif` 路径
- 配置 HTTP 代理并检测可用性

### 自定义背景

- 使用单张图片或视频作为背景
- 去除背景遮罩
- 调整媒体透明度
- 调整前景容器透明度
- 分别为联系人列表、群成员区、消息气泡、输入框启用磨砂效果

### 精简功能

#### 侧边栏

- 可分别控制顶部和底部侧边栏项目显隐
- 默认可管理的项目包括：`消息`、`联系人`、`更多`、`调色盘`、`邮箱`、`文件管理器`、`收藏`、`我的手机`
- 侧边栏中的 `更多` 项通常包含设置入口，关闭前请确认你还有其他方式打开设置页

#### 顶部功能栏

- 首次进入群聊或私聊界面后，插件会自动读取当前可见栏目
- 之后可在设置页中逐项精简显示

#### 聊天功能栏

- 首次进入群聊或私聊界面后，插件会自动读取当前可见栏目
- 之后可在设置页中逐项精简显示

## 已删减说明

`v5` 不再追求覆盖 `v4` 的全部历史能力，而是保留仍在维护、能适配当前结构的一部分功能。

如果你是从 `v4` 迁移过来，看到某些旧功能消失，这是预期行为，不是 README 遗漏。

## 致谢

本项目使用了 [LLOneBot](https://github.com/LLOneBot) 的rkey接口

## 声明

### 一切开发旨在学习，请勿用于非法和商业用途

- 本项目完全免费且开放源代码，仅供学习与交流使用
- 项目不会通过任何方式强制收费
- 插件由开源社区共同维护，贡献者对各自提交内容保留相应权利
- 请勿将本项目用于任何非法用途，由不当使用造成的后果需自行承担
