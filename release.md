# v4.0.4-teambreaker.2

- 修复撤回通知早于消息数据更新时，自己的消息缺少“已撤回”标记和自定义颜色的问题。
- 兼容消息外层容器及同一元素上的多个 Vue 组件，补充已挂载消息和延迟挂载消息的提示刷新。
- 撤回标记和颜色初始化不再等待聊天状态接口，避免旧版 QQ 接口差异阻塞提示。
- 保留现有配置和撤回缓存。新增 `pnpm test:recall`，在独立隐藏的 Electron 窗口中验证标记和颜色。

# v4.0.3

## 更新内容

- 新增 `视频文件消息转小视频消息` 功能
- 新增 `图片附件转普通图片消息` 功能

> [!NOTE]
> 重构后版本号将从 4.0.0 开始
> 如果使用过旧版插件，请删除旧版插件的配置文件和防撤回数据，两者不再兼容
> 插件在 42086 和 49599 下进行调试和开发，无法确保其他未测试版本的功能可用性
> 移除了部分高版本下不稳定的功能
> 后续该插件将不再接受新功能请求
